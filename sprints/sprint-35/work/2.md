# work/2 — `run_reproducibility_demo.py` built + ALL PASS

## What it does (Sprint 35 target #1)
- (a) captures host/platform facts from the LIVE system (uname, python, cpu_count) — printed + emitted
- (b) re-runs the Sprint-34 whole-catalog two-path survey over the 22-org catalog (reusing
  `r34.build_catalog()` == the Sprint-34 catalog builder, `r33._surface`/`_classify`/`_gated_set`,
  the engine advisory `cockpit_q7q8`, `capacity_rerank`), asserting for EVERY org the deterministic
  `two_path_surface` + PATH class EQUALS the Sprint-34 recorded results
- (c) asserts the Sprint-34 consolidated boundary doc's concrete claims against the LIVE engine/module
- emits `artifacts/adjudication/reports/reproducibility.md`; exit 0 = ALL PASS

## Result (real output)
`python3 run_reproducibility_demo.py` → **RESULT: ALL PASS** (exit 0); **40 PASS / 0 FAIL** in the log.

Verified assertions:
- catalog == the recorded 22-org set (labels == union of every CR runner's orgs; no org invented)
- taxonomy **{12,6,4}** == recorded; label membership per class matches; no-capacity orgs carry NO
  capacity_constraint block; best-runnable orgs carry one
- 4 RE-RANK orgs + replacements == recorded Sprint-32/34 (`deli-recommend-infcap`→conditional-resolution,
  `inspect-recorded`→conditional-accept-with-guarantee, `cove-recommend-infcap`→authorize-generic,
  `deli-all-infeasible`→unresolved)
- 22/22 advisory Q8 == `cockpit_q7q8` (never shadowed); 4/4 re-rank replacements distinct from advisory
  Q8 AND machine_eligible_best; 18/18 non-firing orgs agree (replacement == advisory Q8)
- floor integrity 22/22 (against the frozen `rank`)
- two_path_surface identical on re-run (22/22)
- Sprint-31 tally **11/11**, Sprint-32 re-rank **4/4**, Sprint-33 13-org taxonomy **{5,4,4}** all reproduced
- boundary-doc concrete claims: engine sha256 `a60f8f71`, capacity_rerank `f7c6a185`, schema .yaml
  `34264934` (.json `7fc38c8c`), 49 `$defs`, SPEC v0.22 — all PASS

## Code fixes made during build
- SPEC version regex: `**Version:** 0.22` (markdown asterisks) — `/Version:\s*\**\s*([0-9.]+)/`.
- `n_defs` scoping into `run_all` for the report line.
- Report f-string `{5,4,4}` escaped as `{{5,4,4}}` (f-string was parsing it as a tuple).
- Redirected the Sprint-31 `r32.build()` reconstruction to devnull.

## Not-written (0 fixture writes)
`grep -c emit_fixtures` → 2 (both prose mentions in docstring + provenance line; no calls). Engine +
`capacity_rerank.py` UNTOUCHED (hashes read only). Single-threaded; deterministic; ~$0.